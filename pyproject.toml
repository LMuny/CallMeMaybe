[project]
name = "root"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LMuny", email = "ludovic.muny@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "pydantic"
]

[project.scripts]
root = "root:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
